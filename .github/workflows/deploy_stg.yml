name: Deploy Md Promotion Linkage Lambda to Prod

on:
  release:
    types: [published] # リリースノートが公開された時に発火します

jobs:
  deploy:
    # stgブランチをターゲットにして作成されたリリースの場合のみ実行したい場合は、以下のif文のコメントアウトを外してください
    # if: github.event.release.target_commitish == 'stg'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        # 注意: actions/checkout の最新は v4 です（v6はまだ存在しません）
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Check Tag and Deploy
        run: |
          echo "リリースタグを確認しました: ${{ github.event.release.tag_name }}"
          echo "Deploying to production environment..." 
          # ここに実際のデプロイコマンドを追加
